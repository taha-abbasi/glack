# Google OAuth client ID for the Glack Desktop app (created in Google Cloud Console).
# Type: Desktop app (uses PKCE; no client secret needed).
# Wired up in Phase 1; unused in Phase 0 scaffold.
GOOGLE_OAUTH_CLIENT_ID=
